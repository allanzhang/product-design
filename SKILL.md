---
name: product-design
description: 设计、构建或评审 B 端产品界面（SaaS Web 后台、内部工具、微信小程序）时使用。覆盖用户任务导向的信息架构、布局构图、色彩与字体 token、表格/表单/导航/反馈模式、小程序平台约束，以及命名的反模式自检清单。营销/品牌报告页任务按 references/brand-pages/ 下的离线快照处理。
---

# product-design — B 端产品设计规范（SaaS / 小程序）

把界面当作**用户用来完成工作的工具**来设计。用户打开后台是为了审批订单、查库存、提交申请，不是欣赏页面。目标气质：精确、平静、直接、信息密集但不嘈杂；用清晰、证据和掌控感建立信任，不用装饰、噱头、虚假的紧迫感或夸张的文案。

本规范改编自 Vercel design.md 的方法论（单色地基 + 语义色 + token 机制层 + 反模式命名 + 反馈闭环），针对 B 端应用 UI 重写。

## 适用范围

- **适用**：SaaS Web 后台、内部运营工具、数据密集型管理界面；微信小程序及同类移动端 B 端应用。
- **品牌/报告页**（营销落地页、客户报告、提案、benchmark 页、演示 deck）：本规范的产品 UI 规则不直接套用；先读 `references/brand-pages/README.md`（本组织适配规则）。该模式需要 Vercel 公开发布的规范快照与样式表，这两份第三方文件不随仓库分发——若 `references/brand-pages/vercel-design.md` 或 `assets/vercel-brand.css` 不存在，先运行 `bash scripts/fetch-vercel-assets.sh` 拉取，勿凭记忆编造其内容。**排版、token、组件、构图全部照用 Vercel 规范，唯一改动是品牌署名位留空**：不输出 Vercel wordmark 和三角 logo，也不用文字/临时图形填补，等本组织自有品牌资产确定后再填。

## 优先级顺序（需求冲突时，自上而下保护）

1. **事实与数据准确**：数字、单位、金额、状态、权限、时间不得编造、四舍五入误导或美化；保留限定条件。
2. **不破坏宿主框架**：沿用项目已有技术栈、组件库、路由、文件结构与主题机制；只改自然归属的文件，不引入并行 UI 体系。
3. **用户的任务一眼可做**：首屏就是工作本身（列表、表单、操作台），不是欢迎横幅或营销 hero。
4. **层级与产品识别**：通过排版、栅格对齐、克制度建立统一的产品形象，而非靠颜色和装饰。
5. **为当前任务选择构图**：拒绝"所有页面一个模板"，也拒绝模型默认的 generic SaaS 样式。
6. **响应式与细节精修**，但不得削弱已建立的层级。

信息不足时：只有当缺口会改变商业含义、金额、状态、权限、审批流或截止时间时才提问；否则省略未知项并诚实标注，继续推进。

## 工作四遍

### 1. 框定用户的任务（Frame the job）

动手前先明确：谁在什么场景下打开这个界面？他要完成什么动作、依据什么信息做决定？最高频的一条路径是什么？哪些数据是决策依据，哪些只是备查？

- 按用户任务排序，不按数据来源或接口字段顺序排。
- 同时服务两种节奏：**扫一眼**（导航、标题、状态、关键数字、主操作）和**仔细查**（完整表格、明细、日志、备注）。
- 每个区块必须回答一个新的用户问题；重复的结论合并删除。

### 2. 选择构图（Choose composition）

首屏即任务，拒绝该类页面的"默认长相"，除非材料真的需要：

- **决策/审批**：结论与关键依据同屏并列，操作按钮固定在决策区。
- **列表/查找**：筛选与结果同屏，表格是主角，不要用卡片墙代替高密度表格。
- **录入/下单**：表单就是主对象，不要在表单前放仪式性的介绍页。
- **监控/看板**：让异常和趋势引领，精确记录在下方；一屏一个核心问题。
- **对比**：备选项放在同一视觉基准上，差异要"被看到"，而不是靠文字重建。

先定几何结构再选组件：量级→共同刻度上的位置/长度；状态→颜色+图标+文字；流程→连接与顺序；定性选项→对齐的行。

### 3. 用 token 构建（Build with the system）

只使用已发布的 token 与既有组件：不发明字号、字重、颜色、间距、圆角值。判断写在本规范里，可重复的机制沉到 token/CSS/组件里。机械规则（如数字列右对齐、触控目标尺寸）应能被 lint 或代码检查，而不是靠每次记忆。

### 4. 自检与修订（Inspect and revise）

真实渲染后检查，顺序：首屏任务清晰度 → 文案 → 构图 → 排版 → 数据/表格 → 克制度 → 明暗与响应式 → 可访问性。修最高影响的系统性缺陷，重新渲染，直到没有已知的实质问题。自检过程内部完成，交付成果而不是评分报告。

## 参考文件路由（按需读取，不要全量加载）

| 当前任务 | 读取 |
|---|---|
| 定义/使用颜色、字体、间距、圆角、暗色主题 | `references/visual-system.md` |
| SaaS Web：导航、表格、表单、按钮、反馈、图表、详情页 | `references/saas-ui.md` |
| 微信小程序：rpx、导航胶囊、tabBar、触控、原生组件、安全区、性能 | `references/miniprogram.md` |
| 交付前自检、code review 对照 | `references/anti-patterns.md`（命名反模式清单） |
| 营销页/客户报告/提案/benchmark/deck 等品牌表达页 | 先读 `references/brand-pages/README.md`（适配规则：品牌位留空），再用 `references/brand-pages/vercel-design.md`（离线快照，勿联网抓取） |

## 始终生效的硬规则（不可违反）

1. **单色地基**：界面 95% 是中性色。颜色只表达状态、操作或数据，且必须同时配非颜色线索（图标/文字），不许只靠红绿传义。
2. **只用 token**：不写魔法数值；数字用等宽数字（tabular-nums）；订单号/SKU/批次号/ID 用等宽字体，正文不用。
3. **表格**：默认占满内容宽度；文本列左对齐、数字列右对齐且表头随列对齐；同行基线对齐；图表条共享同一刻度。
4. **按钮层级**：一个视图只有一个主按钮；破坏性操作（删除/撤销/驳回）必须二次确认并使用独立的危险样式。
5. **默认静止**：无自动轮播、脉冲、打字机、视差、bounce；动效只用于表达状态变化、连续性或操作确认，并尊重 reduced-motion。
6. **禁止装饰**：渐变/辉光/玻璃拟态/条纹纹理/装饰阴影、卡片套卡片、彩色图标方块、em dash（——）、装饰性全大写眉标与编号。
7. **状态可见**：加载用骨架屏而非无限转圈；操作后有明确反馈（成功 toast / 错误归属到字段）；空状态给出下一步行动。
8. **可访问性**：WCAG AA、可见 focus、语义化标签、明暗双主题层级等价；小程序触控目标 ≥ 88rpx。
9. **平台约束**：小程序避让微信胶囊、优先官方原生组件、底部留安全区；Web 端沿用宿主组件库，不并行造主题。

## 反馈闭环（让本规范越用越准）

评审中重复出现的每条修正，改写成**可观察**的规则（"证据表格占满可用宽度"，而不是"表格别太挤"），并落到最窄的有效层：

- 主观判断 → 写进本规范或对应 reference；
- 可复用机制 → 沉到 token / 组件 / 样式表；
- 机械可判定 → 写成 lint、测试或确定性检查。

新页面类型或新模式反复出现时，新增为参考文件中的模式章节；一条规则如果没让同类投诉减少，说明它表述不清、没被加载或放错了层。目标是 Vercel 式的判断力，不是 Vercel 式的装饰。
